#include<stdio.h>
int main()
{ 
    int length=0;
    printf("Enter the length :");
    scanf("%d",&length);

    int area=length*length;
    printf("area of square %d",area);
    
    return 0;

}